spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/limochat
    username: limochat_user
    password: 0413Maou
    driver-class-name: org.postgresql.Driver

  application:
    name: limochat

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

sql:
  init:
      mode: never